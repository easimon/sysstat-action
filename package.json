{
  "name": "sysstat-action",
  "version": "1.0.0",
  "description": "Early prototype.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && ncc build post.js && mv dist/index.js dist/post.js && ncc build index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/artifact": "^2.2.2",
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "glob": "^11.0.1",
    "json-2-csv": "^5.5.9"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
